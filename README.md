# Astral Routing Engine
A high-performance, scalable routing engine for complex network topologies

## Installation
1. Install Go (version 1.19 or higher)
2. Run `go mod init astral-routing-engine` in the project directory

## Usage
1. Run `go run src/main.go` to start the engine
2. Use a tool like `curl` to test the API endpoints